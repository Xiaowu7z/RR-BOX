package com.rr.client.routing

/** Reviewed app-wide DIRECT identity; domain rules cover connections with no known owner. */
object JarvisAppPolicy {
    const val PACKAGE_NAME = "app.jarvis.assistant"
}
