# 安全与隐私边界

RRBOX 在本机保存节点、订阅、设置和最近已验证的 VPN 运行配置。Android 备份已关闭；PIN 锁不等同于独立加密数据库，不能防御 Root、系统提权或对已解锁设备的完全控制。达到配置的 PIN 错误次数可能清除应用数据，设置时务必阅读提示。

网络请求包括用户配置的代理 / 订阅服务器、规则集来源、GitHub 更新通道以及用户主动运行的诊断目标。普通连接不需要第三方遥测账户，但代理服务方和网络路径仍有其自身可见信息。

优先使用 HTTPS 订阅与经过验证的 TLS。明确输入 HTTPS 时不跟随跨协议重定向；省略协议时保留 HTTPS → HTTP 的自建兼容回退。HTTP 订阅地址与内容以明文传输。“跳过 TLS 证书校验”必须是显式选择，不应作为通用加速选项。

导入有大小 / 数量限制，Clash YAML 使用安全构造器；未知 Raw 字段仍由内置 libbox 校验，不代表任何 JSON 都能运行。节点编辑和 Quick Settings 启动前检查当前配置，日志中心对常见敏感键、URL 与分享链接进行尽力脱敏。脱敏不是匿名化保证，域名、节点名称和自定义字段可能仍可识别用户。

不要把含凭据的配置、日志、截图、APK 签名私钥或管理令牌发到公开 Issue。先去掉敏感内容；如果已经泄露，请撤销 / 轮换相关凭据。项目没有另行公布私密安全报告邮箱，请勿猜测邮箱或向仓库作者公开发送秘密。

本文说明已实现的保护和明确边界，不宣称软件绝对安全或没有未发现漏洞。
