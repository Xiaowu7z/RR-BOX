# 更新记录

## 1.0.4（104）· 预设补齐与选中模式检查

- 扩展精确包名预设，加入 AyuGram、iMe、Nagram、Grok、Chrome、Edge、Google 文档 / 表格 / 幻灯片、Keep、Tasks、日历、通讯录、Assistant、Play Games、Authenticator、Steam、VK、F-Droid 与 Obtainium。仅匹配已安装应用，不泛选所有 Google 或系统组件；Google Home、Wallet 等仍可按需手动添加。
- 明确推荐 Google Play 服务（`com.google.android.gms`，FCM 后台推送）、Google 服务框架及 Play 商店。应用列表增加推送组件状态和标注，自动选择结果显示保留的手动取消项数量；用户仍可继续加选或取消。
- 检查 System / HEV / Root 的“仅选中应用 + 关闭智能分流”：选中应用互联网连接统一走代理，未选应用业务直接联网；快捷开关重连使用当前保存的名单和开关。关闭智能分流后，已选浏览器的国内网页也会走代理。
- 修正 Root 设备 DNS 整地址接管造成的范围扩大：共享解析仅匹配物理 DNS 地址的 TCP / UDP 53，未选应用访问相同地址的网页等其他连接不再误入代理。保留共享 DNS 与共享 UID 的系统边界；系统不支持端口策略规则时安全失败并回滚。
- 增加自动选择、三引擎配置及 Root 收发边界回归。自动化检查不替代 Android 实机后台推送与 ChatGPT 证书问题验收；保持原证书校验与 HTTPS 透传。
- 版本提升为 **1.0.4 / 104**，保持正式包名与签名，可覆盖安装保留配置。

## 1.0.3（103）· 自动选择与 ChatGPT 分流优化

- “仅选中应用”新增“自动选择”，按精确包名匹配已安装的常用海外应用和谷歌服务；保留手动添加与取消，可重复点击补充新安装应用，并清理已卸载项。浏览器和未知应用继续手动选择，不在后台擅自扩大范围。
- 预装应用与谷歌后台服务可在名单中查看和调整；批量保存后只应用一次配置，代理名单和绕过名单独立保存。清空名单不再恢复旧版迁移数据；HEV 显式空允许名单会拒绝启动，不会退回所有应用。
- 依据 [OpenAI 官方网络说明](https://help.openai.com/zh-hans-cn/articles/9247338-network-recommendations-for-chatgpt-errors-on-web-and-apps) 补齐 ChatGPT 登录、文件、验证、配置和帮助服务域名，连接与 DNS 使用同一代理优先策略，覆盖 System / HEV / Root。识别到 ChatGPT 包身份时，公网流量优先代理，局域网仍按原有策略。
- 策略版本 `2026091101` 同步内置 JSON 与签名在线规则，继续兼容 schema 1。网页 WebSocket 使用原有 TCP 443 透传；未添加 TLS 解密或跳过证书校验，证书错误根因仍需结合原始提示和实际网络确认。
- 版本提升为 **1.0.3 / 103**，保持原包名和正式签名，可覆盖安装保留配置。功能按钮需升级 APK；策略数据可通过“立即更新分流规则”获取。历史发布保持不变。

## 1.0.2（102）· JARVIS 助手直连

- 智能分流加入精确包名 `app.jarvis.assistant` 的优先直连规则，位于 DNS 接管之后、X 地址恢复及代理分流之前。仅处理这个明确指定的包名；关闭智能分流时不启用。
- 策略版本 `2026090803` 显式列出 `api.deepseek.com` 的直连连接和 DNS，保留既有 `deepseek.com` 后缀规则。继续使用 schema 1，兼容旧客户端的规则更新。
- JARVIS 包名规则属于 APK 功能，需覆盖安装 `RRBOX-1.0.2-arm64-v8a.apk` 并重新连接；只更新在线规则不能补充该能力。System / Root 依赖应用身份识别；HEV 或身份未知时使用域名策略。应用访问其他服务同样按包名直连。
- 版本提升至 **1.0.2 / 102**，保持包名及正式签名，保留配置覆盖安装；保留 `v1.0.1` 历史发布与已有微信、X、Root 修复。

## 1.0.1（101）· 正式整合版

- 整合此前 1.0.0 测试与修复构建中的 Root 高级模式、国内智能分流、规则在线更新、X 旧目的地址恢复及微信 IPv6 直连恢复，统一发布为 `v1.0.1` 正式版。保留石墨蓝界面、日志持久保存与 TXT 导出、节点分享及分组改名。
- Root 模式按 UID 接管 IPv4 / IPv6，复用稳定内核和应用分流；补齐 System stack 内部 TCP 回程，保留自身出口绕行、局域网访问及独立清理。System 继续默认，无需 Root；HEV 和 Root 均为可选模式。
- “立即更新分流规则”统一更新中国域名 / IP 与自定义域名、包名和精确 IP 策略。规则签名及逐文件哈希校验通过后暂存，新配置启动成功才确认版本；失败恢复旧配置。普通策略数据可独立维护，新增客户端行为仍通过 APK 升级交付。
- X 地址恢复仅针对可识别协议中的已审核精确服务域名及公网目的地址，使用远程 DNS 重新解析，保留端口及原有分流优先级；未知裸 IP、局域网、合成地址和节点入口不做猜测映射。
- 微信恢复仅在 Android 13 及以上的 Root 模式，且物理快照明确具有 IPv4 地址及默认路由、缺少公网 IPv6 地址或默认路由时启用。符合当前直连策略的精确微信服务域名，经 HTTP/TLS 识别或确认微信应用身份后，可将公网 IPv6 TCP 目标重新解析并回退；正常双栈、纯 IPv6、状态未知、较旧 Android 及 System / HEV 沿用原目的地址，所有 UDP 保持原样。该判断不是外网 IPv6 连通性探测。
- 构建验证包含 Release 单元测试、Android Lint、签名 / 版本 / ABI 检查，以及真实核心路由、Root 双栈收发、X 恢复、微信 IPv4 / IPv6 回退和 UDP 不改写检查。以本次 Release 报告为准；这些验证不能替代不同手机、Root 环境、运营商切网及具体应用业务验收。
- 版本由 **1.0.0 / 100** 提升至 **1.0.1 / 101**，包名和正式签名保持一致。已有正式版或同签名修复版可直接覆盖安装并保留本地配置；App 内检查更新可识别新版本，安装后重新连接。下载文件为 `RRBOX-1.0.1-arm64-v8a.apk`。

以下保留 1.0.0 各构建发布时的历史记录；其中“测试构建”“尚未发布正式版”和同版本覆盖安装说明仅描述当时状态。

## 1.0.0（100）· 微信 IPv6 直连恢复修复构建

- 针对日志中切到 Wi-Fi 后微信 IPv6 目的地址不可达的问题，仅在 Root 当前物理网络明确具有 IPv4 路径、缺少可用公网 IPv6 路径时启用精确微信服务域名恢复。正常双栈、纯 IPv6、状态未知以及 System/HEV 模式沿用原目的地址。
- 同时要求智能分流开启、当前域名策略仍为直连、原目标为公网 IPv6；TCP 可识别 HTTP/TLS 或已确认微信应用身份后，使用国内 DNS 重新拨号，保留端口及双栈 TCP 回退。未知裸 IP、所有 UDP（包括通话/P2P）、局域网和代理节点地址不做猜测映射。
- 原微信直连、海外代理、Root 双栈接管、快捷开关与规则热更新继续保留。每次 Root 重建重新判定物理网络，路由表变化也参与切网检测；原始配置缓存保留恢复候选，已有规则快照不依赖先下载新规则。
- 新增保留真实直连出口的核心收发验证，覆盖 IPv6 目标转 IPv4、仅 AAAA 答案及 TCP 双栈回退；另验证 UDP 不被改写。物理能力门控单独验证，构建测试不代替手机微信业务验收。
- 本次需覆盖安装新 APK；版本仍为 1.0.0 / 100，按本次源码提交及 APK SHA-256 区分。

## 1.0.0（100）· 仓库分流规则更新测试构建

- 原位置按钮改为“立即更新分流规则”，统一获取中国域名/IP 与 RRBOX 自定义域名、包名和精确 IP 策略，显示实际规则版本、说明和启用状态。
- 三种引擎从同一不可变策略生成路由和 DNS；Root/System 保留应用身份匹配，HEV 使用同版域名/IP 规则。后续普通规则数据修改无需再次安装 APK。
- 规则使用原发布证书签名，限制格式、版本及大小并逐文件核对 SHA256；完整测试通过后发布独立规则包，原子移动更新频道。加入仓库和镜像下载源，离线保留内置及最近可用版本。
- 在线更新先校验并暂存，保持当前节点和引擎，新引擎实际启动后才确认规则版本；失败清理候选并恢复旧配置。断开、设置变更、取消和进程重启均处理迟到候选。
- 保留 C 石墨蓝与已有功能。规则更新可能短暂重连，启用成功不等同于全部业务成功；正式版 APK 尚未因此发布，需完成手机覆盖安装与三引擎更新复测。

## 1.0.0（100）· BIGO 智能分流修复测试构建

- 智能分流为 BIGO LIVE 的准确包名增加公网代理优先规则，避免无域名连接被国内 IP 规则分配到直连；保留局域网访问和用户设定的应用接管范围。
- 同步补充 BIGO 官方域名及已观察到的三个精确服务域名到路由和远程 DNS 规则。System / Root 可使用应用身份，HEV 使用域名补充，不把无法识别的纯 IP 猜测为 BIGO。
- 增加实际核心的域名 / TCP / UDP / DNS、国内 IP 上的 HTTP / TLS 域名优先级及相邻域名反例检查；关闭智能分流时保留原默认代理行为。
- 延续 Root TCP 回程修复与原后台服务行为。分流正确不等于服务端允许登录，地区提示仍需同节点手机复测确认。

## 1.0.0（100）· Root TCP 回程修复测试构建

- 为 Root 的 System stack 内部 TCP 对端补充 IPv4 / IPv6 精确回程路由，修复 RRBOX 自身出口绕行时握手回包无法返回 TUN 的问题；保留互联网出口防循环和局域网普通选路。
- 新增模拟 Android 无 main 表兜底的真实 System stack 收发测试，覆盖缺失内部回程的反例及双栈 TCP / UDP；原 Root 清理、System 和 HEV 验证继续执行。
- 持久日志按实际核心配置标明运行引擎和网络栈，区分核心已启动与 Root 接管成功，便于切换后定位故障。
- 继续沿用 C 石墨蓝、原按钮与智能分流规则；仍需手机复测 Root + 智能分流、断开恢复和引擎切换。个别 UDP / 443 零下行及应用旧地址需单独复核，不据此宣称所有网络问题已解决。

## 1.0.0（100）· Root 完整接管测试构建

- 设置新增 Root 高级模式，原生辅助进程创建独立 TUN，并把文件描述符交给现有稳定内核；System / HEV 继续沿用原入口及配置。
- 通过 UID 策略路由接管 IPv4 / IPv6，独立路由表避开 Android 和厂商 fwmark；RRBOX 自身出口绕行，局域网地址保留直连。
- Root 下使用原始连接元组查询内核 UID，继续匹配包名规则；无法唯一识别的连接保留未知，不按域名猜测应用身份。
- Root 单独适配 DNS，使用显式上游避免系统解析循环；当前物理 DNS 地址按设备级规则接管，系统共享解析器不承诺逐应用归属。
- 增加启动期限、授权端身份验证、控制通道心跳、独立清理守护和精确回滚；正常断开、应用消失或主辅助进程被终止时释放本次规则。
- 设置切换、快捷开关、网络恢复与清空数据同步处理 Root 生命周期；Root 运行期间暂停原 System / HEV A/B 测试。
- CI 增加隔离 Linux 网络中的真实 TUN、描述符传递、UID 路由、数据包与清理测试。本构建仍需手机验证联网、切网、息屏和 Root 授权环境，不代表性能或兼容性已经通过实测。

## 1.0.0（100）· Root 隔离检查测试构建

- 网络实验室末尾新增可选 Root 隔离测试入口，由 RRBOX 自身请求授权；测试临时、未启用、无地址的 TUN 创建及关闭清理，并导出 TXT 报告。
- 检查 Root 辅助进程自己的 TCP / UDP 套接字诊断能力及透明套接字选项；这些检查不等于已实现其他应用的连接归属识别。
- 探针不安装流量接管规则，不更改系统路由、DNS、SELinux 或当前 VPN；System / HEV 原有服务和分流配置保持原样。
- 内置探针有独立超时，界面区分完成、未通过与结果不完整；本构建尚未提供 Root 正式转发模式，需要手机实测后继续下一阶段。

## 1.0.0（100）· DNS 与分流修复测试构建

- HEV 停用临时合成地址，内部 DNS 通过 sing-box 返回真实地址，减少引擎切换与重启后的旧映射问题；保留原 MTU、缓冲、pipeline 与 TCP Fast Open 参数。
- 为 HEV 内部 DNS 添加精确入站 / 地址 / 端口规则，智能分流、智能 DNS 和轻量模式的组合不会关闭 VPN 自身的解析器。
- 补充有官方服务依据的三个精确国内域名，以及日志中反复出现、归属经核实的三个阿里云 IPv4 /32 直连例外；国际明确域名与 TikTok 包名规则优先。
- 连接日志区分路由观察和结束事件，增加会话 / 连接标识与收发字节；详细模式保留核心警告和错误。未提供的拨号结果、结束原因和最终 IP 不作成功推断。
- 旧应用自行缓存的地址可能需要彻底关闭应用或重启手机一次；不对无法证明来源的共享地址强制改写或整段直连。
- 延续 C 石墨蓝、日志保留 / 导出、节点分享及分组改名；Root 高级模式留待现有模式稳定后再开发。

## 1.0.0（100）· 日志保留与分享测试构建

- 日志默认保留 2000 条，支持预设、自定义数量和持续保留；本地持久保存，全历史筛选并分页查看。
- 日志导出为真正的 TXT 文件，流式写入所有匹配记录；复制限定当前页，大量历史不整体加载到界面。
- 单节点与订阅分组增加二维码、复制链接和系统分享入口，过长或不适合链接表达的配置可保存文件。
- 订阅分组可在节点页和订阅页改名；刷新只更新订阅内容，避免在途更新覆盖用户名称。
- 延续 C 石墨蓝界面和原有分流 / 双引擎连接流程；此构建用于测试，未发布为稳定版本。

## 1.0.0（100）· 石墨蓝测试构建

- 应用统一采用 C 石墨蓝配色，覆盖底部导航、弹窗、扫码页和系统栏；保留原按钮位置与操作流程。
- 明确标记为“勿选”、使用占位地址与端口 9 的流量信息条目显示为完整换行的概览卡，剩余流量按订阅原文突出展示；未知字段保留，不推算缺失数据。
- 这类信息条目不参与节点选择与测速，真实节点继续使用原菜单；普通长节点名支持两行。
- 保留本分支的国内智能分流、TikTok 精确包名补强及连接流向日志。此构建用于测试，尚未发布稳定版本。

## 1.0.0（100）· 源码检查修复构建

此构建替换同一 `v1.0.0` 的 APK，不提高版本号；请按 Release 的源码提交和 SHA-256 识别，不依赖自动更新提醒。

### 修复
- 剪贴板、扫码、二维码图片、文本入口统一识别订阅 / 节点；根地址与认证地址不再静默误判。
- 订阅地址校验与 BOM 处理，保留原始 token 编码；内容大小、节点数量与 YAML 安全解析限制。
- 名称覆盖与连接参数覆盖分离；订阅节点重排 / 唯一身份匹配时保留 ID；不把旧覆盖套到无关节点。
- 本地节点以事务读取最新数据再修改，避免导入、编辑和删除互相覆盖；首页观察数据库变化。
- Raw HTTP / SOCKS / Shadowsocks 等节点的密码与 Shadowsocks 加密方式修改真正写回 outbound。
- 保存编辑前执行 libbox 配置校验；订阅操作加入忙碌保护和失败清理；添加订阅输入框继续自动清空。
- Quick Settings 使用当前节点 / 分流配置核对运行缓存，修复编辑节点后仍连接旧配置；适配 Android 14+ 授权跳转。
- VPN 停止任务与新启动以请求代次隔离；用户断开后拦截排队的恢复请求；运行缓存改为原子写入。
- libbox 持续接收物理默认接口变化；相同接口的新 Network、DNS / 健康变化和完全离线均参与观察。
- HY2 / TUIC 不再默认跳过证书校验；界面和导入保留显式不安全选项。
- 日志脱敏覆盖引号包裹的 JSON 敏感字段、URL 路径 token 和分享链接；更新下载地址限定到本仓库正式资产。

### 工程和说明
- 增加针对以上问题的回归测试，正式流水线增加 Android Lint 和可下载测试报告。
- Release 同步源码 tag 与已校验产物；检查版本、签名指纹、ABI、规则资产、Obtainium 过滤器和远端 APK 哈希。
- 重写安装 / 导入 / 自签证书 / 同版本更新 / 构建边界说明，历史性能数据单独标明。
- System / HEV 核心固定版本、图标和默认引擎不变。

### 验证边界
自动化测试、静态审查与编译不等同于新构建在手机上的连续联网实测。本次未重测历史 A/B 数字；息屏、后台限制、不同 Android 系统和运营商切网仍应在实际设备验收。

### Android 兼容性收尾
- 修复 Android 8/9 的诊断 MTU 获取；快捷授权使用 AndroidX 兼容接口。
- 摄像头改为可选硬件；显式关闭 Android 12+ 应用数据云备份和设备迁移。
- 诊断与流量报告固定数字格式，不随系统区域设置发生漂移。
