# 更新记录

## 1.0.0（100）· Root 完整接管测试构建

- 设置新增 Root 高级模式，原生辅助进程创建独立 TUN，并把文件描述符交给现有稳定内核；System / HEV 继续沿用原入口及配置。
- 通过 UID 策略路由接管 IPv4 / IPv6，独立路由表避开 Android 和厂商 fwmark；RRBOX 自身出口绕行，局域网地址保留直连。
- Root 下使用原始连接元组查询内核 UID，继续匹配包名规则；无法唯一识别的连接保留未知，不按域名猜测应用身份。
- Root 单独适配 DNS，使用显式上游避免系统解析循环；当前物理 DNS 地址按设备级规则接管，系统共享解析器不承诺逐应用归属。
- 增加启动期限、授权端身份验证、控制通道心跳、独立清理守护和精确回滚；正常断开、应用消失或主辅助进程被终止时释放本次规则。
- 设置切换、快捷开关、网络恢复与清空数据同步处理 Root 生命周期；Root 运行期间暂停原 System / HEV A/B 测试。
- CI 增加隔离 Linux 网络中的真实 TUN、描述符传递、UID 路由、数据包与清理测试。本构建仍需手机验证联网、切网、息屏和 Root 授权环境，不代表性能或兼容性已经通过实测。

## 1.0.0（100）· Root 隔离检查测试构建

- 网络实验室末尾新增可选 Root 隔离测试入口，由 RRBOX 自身请求授权；测试临时、未启用、无地址的 TUN 创建及关闭清理，并导出 TXT 报告。
- 检查 Root 辅助进程自己的 TCP / UDP 套接字诊断能力及透明套接字选项；这些检查不等于已实现其他应用的连接归属识别。
- 探针不安装流量接管规则，不更改系统路由、DNS、SELinux 或当前 VPN；System / HEV 原有服务和分流配置保持原样。
- 内置探针有独立超时，界面区分完成、未通过与结果不完整；本构建尚未提供 Root 正式转发模式，需要手机实测后继续下一阶段。

## 1.0.0（100）· DNS 与分流修复测试构建

- HEV 停用临时合成地址，内部 DNS 通过 sing-box 返回真实地址，减少引擎切换与重启后的旧映射问题；保留原 MTU、缓冲、pipeline 与 TCP Fast Open 参数。
- 为 HEV 内部 DNS 添加精确入站 / 地址 / 端口规则，智能分流、智能 DNS 和轻量模式的组合不会关闭 VPN 自身的解析器。
- 补充有官方服务依据的三个精确国内域名，以及日志中反复出现、归属经核实的三个阿里云 IPv4 /32 直连例外；国际明确域名与 TikTok 包名规则优先。
- 连接日志区分路由观察和结束事件，增加会话 / 连接标识与收发字节；详细模式保留核心警告和错误。未提供的拨号结果、结束原因和最终 IP 不作成功推断。
- 旧应用自行缓存的地址可能需要彻底关闭应用或重启手机一次；不对无法证明来源的共享地址强制改写或整段直连。
- 延续 C 石墨蓝、日志保留 / 导出、节点分享及分组改名；Root 高级模式留待现有模式稳定后再开发。

## 1.0.0（100）· 日志保留与分享测试构建

- 日志默认保留 2000 条，支持预设、自定义数量和持续保留；本地持久保存，全历史筛选并分页查看。
- 日志导出为真正的 TXT 文件，流式写入所有匹配记录；复制限定当前页，大量历史不整体加载到界面。
- 单节点与订阅分组增加二维码、复制链接和系统分享入口，过长或不适合链接表达的配置可保存文件。
- 订阅分组可在节点页和订阅页改名；刷新只更新订阅内容，避免在途更新覆盖用户名称。
- 延续 C 石墨蓝界面和原有分流 / 双引擎连接流程；此构建用于测试，未发布为稳定版本。

## 1.0.0（100）· 石墨蓝测试构建

- 应用统一采用 C 石墨蓝配色，覆盖底部导航、弹窗、扫码页和系统栏；保留原按钮位置与操作流程。
- 明确标记为“勿选”、使用占位地址与端口 9 的流量信息条目显示为完整换行的概览卡，剩余流量按订阅原文突出展示；未知字段保留，不推算缺失数据。
- 这类信息条目不参与节点选择与测速，真实节点继续使用原菜单；普通长节点名支持两行。
- 保留本分支的国内智能分流、TikTok 精确包名补强及连接流向日志。此构建用于测试，尚未发布稳定版本。

## 1.0.0（100）· 源码检查修复构建

此构建替换同一 `v1.0.0` 的 APK，不提高版本号；请按 Release 的源码提交和 SHA-256 识别，不依赖自动更新提醒。

### 修复
- 剪贴板、扫码、二维码图片、文本入口统一识别订阅 / 节点；根地址与认证地址不再静默误判。
- 订阅地址校验与 BOM 处理，保留原始 token 编码；内容大小、节点数量与 YAML 安全解析限制。
- 名称覆盖与连接参数覆盖分离；订阅节点重排 / 唯一身份匹配时保留 ID；不把旧覆盖套到无关节点。
- 本地节点以事务读取最新数据再修改，避免导入、编辑和删除互相覆盖；首页观察数据库变化。
- Raw HTTP / SOCKS / Shadowsocks 等节点的密码与 Shadowsocks 加密方式修改真正写回 outbound。
- 保存编辑前执行 libbox 配置校验；订阅操作加入忙碌保护和失败清理；添加订阅输入框继续自动清空。
- Quick Settings 使用当前节点 / 分流配置核对运行缓存，修复编辑节点后仍连接旧配置；适配 Android 14+ 授权跳转。
- VPN 停止任务与新启动以请求代次隔离；用户断开后拦截排队的恢复请求；运行缓存改为原子写入。
- libbox 持续接收物理默认接口变化；相同接口的新 Network、DNS / 健康变化和完全离线均参与观察。
- HY2 / TUIC 不再默认跳过证书校验；界面和导入保留显式不安全选项。
- 日志脱敏覆盖引号包裹的 JSON 敏感字段、URL 路径 token 和分享链接；更新下载地址限定到本仓库正式资产。

### 工程和说明
- 增加针对以上问题的回归测试，正式流水线增加 Android Lint 和可下载测试报告。
- Release 同步源码 tag 与已校验产物；检查版本、签名指纹、ABI、规则资产、Obtainium 过滤器和远端 APK 哈希。
- 重写安装 / 导入 / 自签证书 / 同版本更新 / 构建边界说明，历史性能数据单独标明。
- System / HEV 核心固定版本、图标和默认引擎不变。

### 验证边界
自动化测试、静态审查与编译不等同于新构建在手机上的连续联网实测。本次未重测历史 A/B 数字；息屏、后台限制、不同 Android 系统和运营商切网仍应在实际设备验收。

### Android 兼容性收尾
- 修复 Android 8/9 的诊断 MTU 获取；快捷授权使用 AndroidX 兼容接口。
- 摄像头改为可选硬件；显式关闭 Android 12+ 应用数据云备份和设备迁移。
- 诊断与流量报告固定数字格式，不随系统区域设置发生漂移。
